import math


def szyfrPrzestawieniowy(tekst, d):
    szyfr = []
    przejscie = math.ceil(math.sqrt(d))
    for i in range(d):
        szyfr.append("_")
    n = 1
    for i in n, n*n<


tekst = ["A", "L", "G", "O", "R", "Y", "T", "M"]
szyfrPrzestawieniowy(tekst, 8)
